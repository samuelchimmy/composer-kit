{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@composer-kit/docs^... && changeset publish",
    "storybook": "turbo run dev --filter @composer-kit/storybook",
    "run:docs": "turbo run dev --filter @composer-kit/docs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "prettier": "^3.2.5",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@10.28.2",
  "name": "composer-kit",
  "pnpm": {
    "overrides": {
      "@types/react": "^18.3.22",
      "@types/react-dom": "^18.3.7"
    }
  }
}
